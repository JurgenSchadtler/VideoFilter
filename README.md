# VideoFilter
Project made with MatLab applying filters to a video output which is the webcam
Contains:
-Lineal Filter
-Laplace Filter
-Low Pass Filter
-High Pass Filter

Also contains a function to detect the dominant color and reduce the brightness using an exponential function
